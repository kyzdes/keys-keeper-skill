{
  "name": "keys-keeper",
  "version": "0.9.0",
  "description": "Cross-platform secrets workflow for macOS, Windows, and Linux. The `keys` CLI routes values to explicit local sinks without returning plaintext in normal agent tool output. It also organizes local folders and explicit project scopes so approved workers receive only assigned entries and contributors can submit new entries without editing existing ones. This reduces accidental transcript exposure; it is not an isolation boundary against arbitrary code running as the same OS user.",
  "author": {
    "name": "Viacheslav Kuznetsov",
    "url": "https://github.com/kyzdes"
  },
  "homepage": "https://github.com/kyzdes/keys-keeper-skill",
  "repository": "https://github.com/kyzdes/keys-keeper-skill",
  "license": "MIT",
  "keywords": [
    "secrets",
    "keychain",
    "codex",
    "security",
    "developer-tools"
  ],
  "skills": "./skills/",
  "interface": {
    "displayName": "Keys Keeper",
    "shortDescription": "Route local secrets and explicit project scopes without printing values.",
    "longDescription": "Use natural-language requests to list secret metadata, organize folders, explicitly assign entries to project scopes, inject a named key into a file, copy it with automatic clearing, open SSH sessions, and inspect audit history through the local keys CLI. Approved worker profiles receive only assigned entries and contributors submit new entries without editing existing ones. This reduces accidental transcript exposure; it is not an isolation boundary against code running as the same OS user.",
    "developerName": "Viacheslav Kuznetsov",
    "category": "Developer Tools",
    "capabilities": [
      "Inspect secret metadata without revealing values",
      "Route named secrets to explicit local sinks",
      "Open SSH sessions from saved credentials",
      "Audit secret operations",
      "Organize local folders and explicitly assign entries to project scopes",
      "Use scoped read/create worker profiles without master catalog edits"
    ],
    "defaultPrompt": [
      "List my available keys without revealing their values.",
      "Put my saved OpenRouter key into this project's .env file.",
      "Show which entries are explicitly assigned to this project's production scope."
    ],
    "brandColor": "#D97757"
  }
}
