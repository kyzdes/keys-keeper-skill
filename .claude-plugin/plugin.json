{
  "name": "keys-keeper",
  "version": "0.2.0",
  "description": "Cross-platform (macOS + Windows) secrets manager skill: stores API keys, SSH keys, server creds, and domain info in the OS-native credential store (macOS Keychain / Windows Credential Manager) via the `keys` CLI. Agents architecturally cannot reveal plaintext (Sealed wrapper + env-gated reveal). Works with Claude Code and Codex CLI.",
  "author": {
    "name": "kyzdes"
  }
}
